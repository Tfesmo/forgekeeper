/**
 * Passes the input line through unchanged.
 * @param {string} line - The input line to pass through.
 * @returns {string} The unchanged input line.
 */
export function passthrough(line) {
  return line;
}
