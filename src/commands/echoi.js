/**
 * Converts input line to lowercase.
 * @param {string} line - The input line to convert.
 * @returns {string} The lowercased input line.
 */
export function echoi(line) {
  return line.toLowerCase();
}
